# 🔄 更新指南 - 保留所有配置

## ❌ 之前的问题
每次执行 `docker compose up -d --build` 都会重新构建镜像，导致：
- IDEA 设置丢失
- VNC 密码重置 
- Firefox 书签清空
- 桌面文件消失
- 项目配置重置

## ✅ 现在的解决方案

### 数据持久化目录
所有重要配置现在都通过 Docker 数据卷持久化保存：

```
用户配置持久化目录：
├── /home/developer/.config    → IDEA/系统配置
├── /home/developer/.cache     → 应用缓存
├── /home/developer/.local     → 本地应用数据
├── /home/developer/.vnc       → VNC密码和设置
├── /home/developer/.mozilla   → Firefox配置和书签
├── /home/developer/Desktop    → 桌面文件和快捷方式
├── /home/developer/.idea      → IDEA全局配置
├── /home/developer/.java      → Java和JVM配置
└── /home/developer/projects   → 项目文件（共享）
```

### 安全更新方法

#### 方法1：仅重启容器（推荐）
```bash
# 如果只是修改了配置文件（如 .env, nginx.conf, index.html）
docker compose restart
```

#### 方法2：重新创建容器但保留数据
```bash
# 停止容器但保留数据卷
docker compose down

# 重新启动，只有代码修改时才重新构建
docker compose up -d --build
```

#### 方法3：强制重新构建（镜像层修改）
```bash
# 如果修改了 Dockerfile.vnc 内容
docker compose down
docker compose build --no-cache
docker compose up -d
```

### 🔒 数据永久保留
- **Docker数据卷不会被删除** - 即使容器重新构建
- **配置文件自动恢复** - 下次启动时自动加载
- **项目文件共享** - 两个工作区共享项目目录

### 📋 数据卷管理命令

```bash
# 查看所有数据卷
docker volume ls | grep idea

# 备份用户配置（可选）
docker run --rm -v idea-config-user1:/data -v $(pwd):/backup alpine tar czf /backup/user1-config.tar.gz -C /data .

# 恢复用户配置（可选）
docker run --rm -v idea-config-user1:/data -v $(pwd):/backup alpine tar xzf /backup/user1-config.tar.gz -C /data
```

### ⚠️ 注意事项

1. **首次配置后立即生效** - 所有设置都会自动保存
2. **VNC密码修改会保留** - 不会重置为默认密码
3. **IDEA激活状态保持** - 无需重新激活
4. **Firefox扩展和书签保留** - 个人浏览器配置不丢失

### 🚀 最佳实践

- 每次更新前先执行 `docker compose down`
- 避免使用 `docker system prune -a`（会删除数据卷）
- 定期备份重要项目到 `projects` 目录
- 大的环境变更时建议先备份数据卷
